# comment test
worker_processes 3;
event {
    worker_connections 12;
    use io_uring;
}
http {
    server {
        listen 80;
        server_name "rps.feixingxr.com";
        location /api {
            proxy_pass  
        }
    }
}