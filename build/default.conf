# comment test
worker_processes 1;
event {
    worker_connections 512;
    use epoll;
}

http {
    server {
        listen 8000;
        server_name "rps.feixingxr.com";
        location / {
            proxy_pass "http://127.0.0.1:8080";
        }
    }
}